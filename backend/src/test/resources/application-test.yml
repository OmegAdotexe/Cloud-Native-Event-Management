spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
  rabbitmq:
    host: localhost
    port: 5672

jwt:
  secret: test_super_secret_jwt_key_that_is_long_enough_for_hs256_alg
  expiration-ms: 86400000
